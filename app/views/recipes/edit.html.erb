<div class="container">
  <h1>Modifier la recette</h1>

  <div>
    <%= link_to "Revenir à la recette", recipe_path(@recipe) %>
  </div>

  <div>
    <%= link_to "Revenir à la liste des recettes", recipes_path %>
  </div>

  <%= render "form", recipe: @recipe %>
</div>
